/**
 * Payloads de request/response de la API REST, separados de las entidades de persistencia
 * ({@link emc.mapIt.entity}). Los controllers exponen y aceptan estos tipos, nunca las entidades
 * directamente.
 */
package emc.mapIt.dto;
