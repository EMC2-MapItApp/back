/**
 * Configuración de Spring (seguridad, MongoDB, filtros) y seeders de arranque
 * ({@link org.springframework.boot.CommandLineRunner}) que pueblan datos de referencia
 * (categorías, usuario admin) — revisar antes de asumir que un MongoDB recién creado está vacío.
 */
package emc.mapIt.config;
