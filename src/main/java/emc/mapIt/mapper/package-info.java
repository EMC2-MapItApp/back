/**
 * Mapeo manual entidad ↔ DTO (sin MapStruct). Antes de añadir un mapper nuevo, comprobar que la
 * lógica no vive ya inline en el servicio correspondiente (ver BITACORA.md — caso
 * {@code CategoryMapper}, eliminado por duplicar sin usarse la lógica real del servicio).
 */
package emc.mapIt.mapper;
