/**
 * Repositorios de Spring Data MongoDB, uno por entidad de {@link emc.mapIt.entity}. Solo
 * declaran consultas derivadas del nombre del método o anotadas con {@code @Query}; no hay
 * lógica de negocio aquí (eso vive en {@link emc.mapIt.service}).
 */
package emc.mapIt.repository;
