/**
 * Documentos de MongoDB (Spring Data MongoDB) que forman el modelo de dominio de MapIt: usuarios,
 * lugares, publicaciones y el árbol de categorías (main category → subcategory → location type),
 * más las entidades del sistema de niveles/XP/capacidades (en progreso).
 */
package emc.mapIt.entity;
